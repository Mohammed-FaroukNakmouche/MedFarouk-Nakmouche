# Academic website redesign

This redesign follows the content architecture of the Zheng Liu academic site while keeping Mohammed Farouk Nakmouche's own content and branding.

## Files to copy into the repository root

- `index.html` — replace current homepage
- `research.html` — new research overview page
- `publications.html` — new categorized publications page
- `projects.html` — new research gallery page
- `assets/styles.css` — replace current stylesheet
- `assets/script.js` — replace current script

Keep these existing repository files unchanged:

- `Images/profile.png`
- `cv.pdf`
- `data/profile.js` (the redesigned pages do not currently depend on it, so it can remain for future structured content use)

## Suggested next update

Replace the abstract project-card diagrams in `projects.html` with real CST screenshots, prototype photos, measurement setups, or paper figures. The cards are already structured for a visual gallery.
